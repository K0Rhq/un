---
import { getCollection } from "astro:content";

const items = await getCollection("docs");
const firstItem = items.find((item) => !item.data.directory);
return Astro.redirect(`/docs/${firstItem.id}`);
---
